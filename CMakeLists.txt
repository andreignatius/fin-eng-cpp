cmake_minimum_required(VERSION 3.10)  # Specify the minimum version of CMake

project(HelloWorld)  # Define the project name

# Create an executable target called "HelloWorld" based on the source file "main.cpp"
add_executable(HelloWorld main.cpp)
