#ifndef UTILS_H
#define UTILS_H

#include "Types.h" // This includes the definition of OptionType and the declaration of optionTypeNames

// Convert OptionType enum to its corresponding string representation
std::string OptionTypeToString(OptionType type);

#endif // UTILS_H
